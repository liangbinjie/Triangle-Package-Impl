let
  from Utils import min, max;
  from MathEx import square;
  proc main() ~
    begin
      putint(min(5, 9)); puteol();   ! -> 5
      putint(max(5, 9)); puteol();   ! -> 9
      putint(square(7)); puteol()    ! -> 49
    end
in
  main()
